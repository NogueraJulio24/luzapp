<div class="card">
  <div class="card-header" data-background-color="purple">
    <h3 class="title text-center">Editar Marca</h3>
  </div>
  <div class="container">
      <%= render 'form', brand: @brand %>
  </div>
</div>

<%= link_to 'Mostrar', @brand, class: 'btn btn-success' %> 
<%= link_to 'Atrás', brands_path, class: 'btn btn-success' %>
